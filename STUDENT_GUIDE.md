# 学生指南：如何用这张 3D 地图找到你的论文选题

> 写给正在找研究方向、准备开题、或者想知道"agent + 代码生成"这个领域到底在干什么的学生。

---

## 你可能遇到过这些问题

| 问题 | 这张地图怎么帮你 |
|------|-----------------|
| "我想做 agent 相关研究，但方向太多了不知道选哪个" | 打开立方体，一眼看到 10 个场景 × 7 个方向的全景，比读 100 篇综述快 |
| "开题报告要写 Research Gap，但不知道哪里还没人做" | 点开任何立方体，标着"暂无论文——缺类"的就是没人做过的位置 |
| "导师让我调研某个方向，我该从哪些论文开始读？" | 点击对应的立方体，里面有该方向的关键论文，按引用量和时间排序 |
| "这篇论文和我读的那篇是什么关系？" | 每篇论文有 8 个 tag（场景/方向/层级/象限/oracle/域/反馈栈/自指），用过滤器找同类 |
| "我想发一篇有影响力的论文，哪里竞争少但潜力大？" | 空白格 + 旁边已有正主的格子 = "这个方向有人在做但这个角度还没人做" |

---

## 3 分钟理解这个领域

**一句话**：这个领域研究的是——LLM agent 怎么用**可执行代码**来感知世界、做决策、自我改进。

**打个比方**：如果说 LLM 是一个聪明的大脑，那代码就是它的手——大脑想"我要抓那个杯子"，手（代码）才能真正伸出去抓。这个领域研究的就是：这只手能做什么、怎么做得更好、怎么自己学会新手。

**10 个场景**（agent 用代码做什么）：

```
策略 Policy     → 让机器人动起来（控制代码）
动作 Action     → 操作电脑/手机（GUI 自动化）
奖励 Reward     → 设计训练信号（奖励函数代码）
技能 Skill      → 积累可复用的能力（工具库）
约束 Constraint → 保证安全（监控/护栏代码）
世界 World      → 构建可模拟的环境（世界模型）
设计 Design     → 生成 CAD/本体/法规/前端
推理 Reasoning  → 用程序思维解题（数学/逻辑）
Agent 自身      → 改进自己的架构（harness）
科研 Research   → 自动做研究（AI Scientist）
```

**为什么用 3D 立方体而不是论文列表**：因为这个领域不是线性的——一篇论文可以同时属于"策略"场景和"进化搜索"方向，还可以涉及"世界模型"——列表没法表达这种多维关系，但立方体可以。

---

## 怎么找到你的选题（实操指南）

### 第一步：打开立方体

```bash
git clone https://github.com/ExuberantWitness/awesome-code-as-agent.git
cd awesome-code-as-agent
start code-as-policy-cube.html    # Windows
open code-as-policy-cube.html     # Mac
```

### 第二步：找到你感兴趣的行（场景）

旋转立方体，X 轴是场景。如果你对机器人感兴趣 → 找到"策略 Policy"那一行；对 AI 安全感兴趣 → 找到"约束 Constraint"。

### 第三步：看哪个格子是空的

点击某个立方体：
- **有论文的格子**：显示论文列表，按四个运行时象限分组
- **标"暂无论文——缺类"的格子**：这就是没人做过的位置

### 第四步：判断这个空格值不值得做

不是所有空格都值得做。检查三个条件：

1. **旁边有正主吗？** 如果相邻格子有论文，说明这个方向有人在做，只是这个角度还没人碰 → 好信号
2. **有免费的验证器吗？** 如果这个域自带便宜的形式化 oracle（如数学域的 Lean 推理机、CAD 域的几何检查器），验证成本低 → 好信号
3. **是结构性空白吗？** 有些格子空是因为"定义上不可能有"（比如"技能×单次生成"——技能的定义就要求复用）→ 避开

### 实例：三个已经被验证的选题发现

| 选题 | 怎么发现的 | 后来被谁做了 |
|------|-----------|-------------|
| 约束×技能沉淀（安全监控器库） | 二维矩阵时代就发现全空白 | MineEvolve (2026) 填了游戏域，机器人域仍空 |
| 世界模型×技能沉淀（想象空间技能库） | 五轮定向检索确认空白 | LUCID + DADS (2026) 从世界模型方向填入 |
| Agent 自身×世界模型 | 立方体新开 wm 方向后暴露 | 至今仍空——可能是你的机会 |

---

## 论文 tag 速查

每篇论文有 8 个 tag，点击立方体或搜索数据库时你会看到：

| Tag | 含义 | 例子 |
|-----|------|------|
| 场景 | 这篇论文在做什么 | policy = 做机器人控制 |
| 方向 | 用什么方法做 | evo = 进化搜索 |
| 层级 | 改进落在哪里 | L1 = 不改权重但改工作流 |
| 象限 | 运行时行为 | 🟡B = 运行时采集数据但不训练 |
| oracle | 反馈从哪来 | sym = 编译器/测试/推理机 |
| 垂直域 | 应用在哪个行业 | robot = 机器人 |
| 反馈栈 | 反馈质量 | ★★★ = 有数据飞轮 |
| 自指 | 是否改进自己 | 🪞 = 改进对象包含改进者 |

---

## 给不同背景学生的建议

### 机器人/控制背景
看 **策略行** 和 **世界行**——你的领域知识在"策略×世界模型"交叉处最值钱。真机经验的人做"世界模型验证"有天然优势。

### NLP/代码生成背景
看 **动作行** 和 **Agent 自身行**——你对 LLM 和 prompt 的理解直接适用。"Agent 自身×记忆上下文"正在爆发（4 轮调研 25 篇）。

### 数学/形式化背景
看 **推理行** 和 **约束行**——形式化验证（Lean/Coq/TLA）是这个领域最稀缺的 oracle。你可以做"世界模型的形式化验证"这种别人做不了的事。

### 安全/对齐背景
看 **约束行**——这个行是全表最薄的（18/70 格空），而且"安全记忆库"（AIR/AgentDebug）刚开了头。RSI 的安全监控（SAHOO）是全新方向。

### 设计/工程背景
看 **设计行**——CAD×进化（EvoCAD/CADEvolve）和法规即代码（Catala）都是刚填入的新格子，垂直域知识在这里非常值钱。

---

## FAQ

**Q：为什么有些论文出现在多个格子里？**
A：因为一篇论文可以组合多个策略。例如 ENPIRE 同时使用"多智能体"和"闭环调整"，所以出现在两个格子里——这是正常现象，我们在 tag 系统里用 ⛓ 标记组合策略。

**Q：立方体里的"缺类"是什么意思？**
A：点开一个立方体后，论文按四个运行时象限分组。如果某个象限标着"暂无论文——缺类"，说明该场景×方向×象限的组合还没有论文——这是一个可能的选题位置。

**Q：这些论文都经过验证吗？**
A：我们用三级验证：✅ 会话核验（直接打开过 arXiv 页面）、🔍 多源确认（搜索结果命中官方页面）、⚠️ 既有知识（高置信记忆但未复核）。数据库视图可以按验证等级过滤。

**Q：我可以贡献论文吗？**
A：可以！参见 [CONTRIBUTING.md](CONTRIBUTING.md)——编辑 `data/papers.json`，按 tag 格式添加，提交 PR 即可。

---

> **最后一句话**：与其在 Google Scholar 上漫无目的地搜，不如打开立方体转一转——你会发现整个领域的结构和空白，比你想象的清晰得多。
